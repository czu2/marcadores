<%= form_with(model: bookmark, remote: true) do |form| %>
  <% if bookmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookmark.errors.count, "error") %> prohibited this bookmark from being saved:</h2>

      <ul>
      <% bookmark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">

    <div class="field">
      <%= form.text_field :url, class:"input-group my-3", placeholder: 'URL' %> 
      <p>Estado: <%= form.select :status, Bookmark.statuses.keys %></p>
    </div>

    <div class="actions">
      <%= form.submit 'Guardar', class: "btn btn-dark text-white", data: { disabled_with: 'Guardando...' } %>
    </div>

  </div>
<% end %>
